# ============================================
# NFT MARKETPLACE BACKEND - ENVIRONMENT VARIABLES
# ============================================
# 
# INSTRUCTIONS:
# 1. Copy this file to .env: cp example.env .env
# 2. Replace all placeholder values with your actual credentials
# 3. NEVER commit .env to version control (it's in .gitignore)
# 4. Use strong, unique values for production environments
#
# ============================================

# ============================================
# CORE APPLICATION CONFIGURATION (REQUIRED)
# ============================================

# Application Environment
# Options: development | test | production | prod
# - development: Development mode with verbose logging
# - test: Testing environment
# - production/prod: Production mode with optimized settings
NODE_ENV=development

# Server Port
# The port on which the API server will listen
# Default: 9090
PORT=9090

# ============================================
# AUTHENTICATION & SECURITY (REQUIRED)
# ============================================

# JWT Secret Key
# ⚠️ CRITICAL: Use a strong, random secret in production!
# Generate with: openssl rand -base64 32
# Minimum 32 characters recommended
# This secret is used to sign and verify JWT tokens
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# JWT Token Expiration
# Format: <number><unit> (e.g., 7d, 24h, 30m)
# Common values: 7d (7 days), 24h (24 hours), 1h (1 hour)
# Default: 7d
JWT_EXPIRES_IN=7d

# ============================================
# DATABASE CONFIGURATION (REQUIRED)
# ============================================

# Primary Database Connection URL
# ⚠️ REQUIRED: Must be a valid PostgreSQL connection string
# 
# Option 1: Neon (Recommended - Free Tier)
# - Sign up: https://console.neon.tech (free, no credit card)
# - Create project → Copy connection string
# - Format: postgresql://username:password@ep-xxx-xxx.region.aws.neon.tech/neondb?sslmode=require
# - Free tier includes: 3 projects, 0.5GB storage, unlimited compute
#
# Option 2: Supabase
# - Dashboard: https://app.supabase.com
# - Settings → Database → Connection pooling
# - Format: postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres?pgbouncer=true
#
# Option 3: Local PostgreSQL
# - Format: postgresql://username:password@localhost:5432/database_name
DATABASE_URL=postgresql://username:password@ep-xxx-xxx.region.aws.neon.tech/neondb?sslmode=require

# Shadow Database URL (Optional but Recommended)
# Used by Prisma for safe migrations in production
# For Neon: Use the same connection string as DATABASE_URL
# For Supabase: Use direct connection (not pooling)
# Format: postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
# If not provided, Prisma will use DATABASE_URL (may cause issues in production)
SHADOW_DATABASE_URL=postgresql://username:password@ep-xxx-xxx.region.aws.neon.tech/neondb?sslmode=require

# ============================================
# FRONTEND & CORS CONFIGURATION (REQUIRED)
# ============================================

# Frontend Application URL
# Used for CORS configuration and Socket.io connections
# Development: http://localhost:19006 (or your Expo/React Native port)
# Production: https://yourdomain.com
# Multiple origins: Use comma-separated values (if supported)
FRONTEND_URL=http://localhost:19006

# ============================================
# EMAIL SERVICE (OPTIONAL)
# ============================================
# Configure if you need to send transactional emails
# (password reset, verification, notifications, etc.)

# SMTP Email Address
# The email address that will send emails to users
# For Gmail: Use your Gmail address
# For other providers: Use your SMTP-enabled email
SMTP_EMAIL=your-email@gmail.com

# SMTP App Password
# ⚠️ NOT your regular email password!
# For Gmail: Generate App Password at https://myaccount.google.com/apppasswords
# For other providers: Check their documentation for app-specific passwords
# Format: Usually 16 characters (e.g., abcd efgh ijkl mnop)
SMTP_PASSWORD=your-app-password

# ============================================
# OAUTH PROVIDERS (OPTIONAL)
# ============================================

# Google OAuth 2.0 Configuration
# Enable Google Sign-In functionality
# Setup: https://console.cloud.google.com/apis/credentials
# 1. Create OAuth 2.0 Client ID
# 2. Add authorized redirect URIs
# 3. Copy Client ID and Client Secret here

# Google OAuth Client ID
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Google OAuth Client Secret
# ⚠️ Keep this secret! Never expose in client-side code
GOOGLE_CLIENT_SECRET=your-google-client-secret

# ============================================
# BLOCKCHAIN & CRYPTO CONFIGURATION (OPTIONAL)
# ============================================

# Tron Network RPC URL
# Used for TRC20 wallet operations and transactions
# Mainnet: https://api.trongrid.io
# Testnet: https://api.shasta.trongrid.io
# Custom: Your own Tron node RPC endpoint
TRON_RPC_URL=https://api.trongrid.io

# ============================================
# SMS SERVICE (OPTIONAL)
# ============================================
# Configure if you need to send SMS notifications
# Currently supports Pindo SMS service

# Pindo API Key
# Get your API key from: https://pindo.io
# Sign up → Dashboard → API Keys
# PINDO_API_KEY=your-pindo-api-key

# Enable SMS Sending
# Set to 'true' or '1' to enable SMS functionality
# Set to 'false' or '0' to disable (default)
# ALLOW_SEND_MESSAGE=false

# ============================================
# ADMIN CONFIGURATION (OPTIONAL)
# ============================================
# Configure super admin credentials for initial setup
# ⚠️ Change these values immediately after first use!

# Super Admin Password
# Password for the super admin account
# Use a strong password (min 12 characters, mixed case, numbers, symbols)
SUPER_ADMIN_PASSWORD=change-this-password

# Super Admin Logic Code
# Additional security code for admin operations
# Should be a unique, hard-to-guess value
SUPER_ADMIN_LOGIC_CODE=change-this-code
